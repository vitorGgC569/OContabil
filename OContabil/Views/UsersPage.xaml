<UserControl x:Class="OContabil.Views.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI" Loaded="OnLoaded">
    <StackPanel>
        <!-- Toolbar -->
        <DockPanel Margin="0,0,0,20">
            <TextBlock Text="Gerencie permissoes e acessos de cada usuario" FontSize="12"
                       Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
            <Button DockPanel.Dock="Right" Style="{DynamicResource BtnPrimary}"
                    Content="Novo Usuario" Click="OnAdd" HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- Table -->
        <Border Style="{DynamicResource Card}" Padding="0">
            <DataGrid x:Name="gridUsers" AutoGenerateColumns="False" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0"
                      Foreground="{DynamicResource TextPrimary}"
                      FontSize="12" RowBackground="Transparent" AlternatingRowBackground="Transparent"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource Border}"
                      HeadersVisibility="Column" CanUserResizeRows="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource BgElevated}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                        <Setter Property="FontSize" Value="10"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="14,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="14,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="NOME" Binding="{Binding FullName, Mode=OneWay}" Width="*"/>
                    <DataGridTextColumn Header="USUARIO" Binding="{Binding Username, Mode=OneWay}" Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="PERFIL" Binding="{Binding RoleDisplay, Mode=OneWay}" Width="130"/>
                    <DataGridTextColumn Header="STATUS" Binding="{Binding StatusStr, Mode=OneWay}" Width="80"/>
                    <DataGridTextColumn Header="CRIADO EM" Binding="{Binding CreatedStr, Mode=OneWay}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Permissions Reference -->
        <Border Style="{DynamicResource Card}" Margin="0,20,0,0">
            <StackPanel>
                <TextBlock Text="Niveis de Acesso" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{DynamicResource BgElevated}"
                            CornerRadius="6" Padding="16" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="Administrador" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                            <TextBlock Text="Acesso total ao sistema" FontSize="11"
                                       Foreground="{DynamicResource TextMuted}" Margin="0,0,0,8"/>
                            <TextBlock Text="• Gerenciar usuarios" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Importar e validar documentos" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Cadastrar clientes" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Alterar configuracoes" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{DynamicResource BgElevated}"
                            CornerRadius="6" Padding="16" Margin="4,0">
                        <StackPanel>
                            <TextBlock Text="Operador" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                            <TextBlock Text="Operacoes do dia-a-dia" FontSize="11"
                                       Foreground="{DynamicResource TextMuted}" Margin="0,0,0,8"/>
                            <TextBlock Text="• Importar documentos" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Validar documentos" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Cadastrar clientes" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Sem acesso a usuarios" FontSize="11" Foreground="{DynamicResource TextMuted}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource BgElevated}"
                            CornerRadius="6" Padding="16" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Visualizador" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                            <TextBlock Text="Apenas consulta" FontSize="11"
                                       Foreground="{DynamicResource TextMuted}" Margin="0,0,0,8"/>
                            <TextBlock Text="• Visualizar documentos" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Visualizar clientes" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="• Sem importacao" FontSize="11" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="• Sem validacao" FontSize="11" Foreground="{DynamicResource TextMuted}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
